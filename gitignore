# Flask backend
/flask-api/__pycache__/
/flask-api/venv/
*.pyc

# React frontend dependencies (but NOT build inside flask-api)
/react-app/node_modules/
/react-app/dist/
/react-app/build/   # ignore only React’s own build
.env.local
.env.production
.env.development

# Ignore root-level Node files (we only want react-app)
/package.json
/package-lock.json
/postcss.config.js
/tailwind.config.js

# OS files
.DS_Store
Thumbs.db
